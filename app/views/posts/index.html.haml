%h1 Listing posts

%table.table 
  %tr
    %th Title
    %th Body

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.body
%br
= link_to 'New Post', new_post_path, class: "btn btn-primary"
